[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short
markers =
    consistency: Tests for parameter consistency
    validation: Tests for result validation
    oceanic: Tests for oceanographic ranges
    edge_case: Tests for edge cases and error handling
